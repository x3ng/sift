[package]
name = "sift_ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
sift = { path = "../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
